export interface CardProps {
  imgSrc: string;
  title: string;
  description: string;
  githubURL: string;
  demoURL: string;
}

export interface Project {
  id: number;
  imgSrc: string;
  title: string;
  description: string;
  githubURL: string;
  demoURL: string;
}
